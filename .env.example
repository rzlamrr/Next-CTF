# Database
DATABASE_URL="file:./dev.db"

# For Production with Supabase (uncomment and replace with your values when using Supabase)
# DATABASE_PROVIDER="postgresql"
# DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"
# DIRECT_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"
# SHADOW_DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"

# NextAuth.js
NEXTAUTH_SECRET="your-generated-secret"
NEXTAUTH_URL="http://127.0.0.1:4000"

# Application
NODE_ENV="development"
NEXT_PUBLIC_BASE_URL="http://127.0.0.1:4000"

# Storage
# Select storage backend: 'local' or 'supabase'
STORAGE_DRIVER="local"

# Local storage directory (for uploads)
UPLOAD_DIR=".uploads"

# Supabase storage (only if STORAGE_DRIVER="supabase")
# Supabase project URL and Service Role Key are required to perform server-side operations
# Bucket defaults to "challenge-files"
# Note: Ensure the bucket exists in Supabase Storage (see README)
SUPABASE_URL=""
SUPABASE_SERVICE_ROLE_KEY=""
SUPABASE_BUCKET="challenge-files"

# Production (Supabase) example:
# STORAGE_DRIVER="supabase"
# SUPABASE_URL="https://<YOUR-PROJECT-REF>.supabase.co"
# SUPABASE_SERVICE_ROLE_KEY="<YOUR-SERVICE-ROLE-KEY>"
# SUPABASE_BUCKET="challenge-files"
#
# Database (Supabase Postgres) example:
# DATABASE_URL="postgresql://postgres:<YOUR-PASSWORD>@db.<YOUR-PROJECT-REF>.supabase.co:5432/postgres"
# DIRECT_URL="postgresql://postgres:<YOUR-PASSWORD>@db.<YOUR-PROJECT-REF>.supabase.co:5432/postgres"
# SHADOW_DATABASE_URL="postgresql://postgres:<YOUR-PASSWORD>@db.<YOUR-PROJECT-REF>.supabase.co:5432/postgres"
# SMTP Email (Notifications)
# Configure these to enable email delivery for admin broadcasts.
# If NOTIFICATIONS_EMAIL_ENABLED is false or these are missing, email sending is skipped safely.
EMAIL_HOST=""
EMAIL_PORT=""      # e.g. 587 for STARTTLS, 465 for SMTPS
EMAIL_USER=""
EMAIL_PASS=""
EMAIL_FROM=""      # e.g. "CTF Admin <no-reply@yourdomain.com>"
NOTIFICATIONS_EMAIL_ENABLED="true"